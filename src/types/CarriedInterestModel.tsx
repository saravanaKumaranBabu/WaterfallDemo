export type CarriedInterestModel = {
    preferredReturn: number, 
    fullPromote: number, 
    catchUpRate: number, 
    catchUpEnd: number, 
    ClawBack: string, 
    frontendClawBack: number, 
    backendClawBack: number
}